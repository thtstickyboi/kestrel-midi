# Comments extracted from src/phase/tests.rs
#
# Numbers are positions in the shipped file, counted from the top.
# They are regenerated for every release and are NOT stable across
# versions -- these files ship beside the code they belong to, so
# they always match it. The development tree keeps every comment
# inline; its history is the place to compare versions.

[1] src/phase/tests.rs:315
    // Generated by compiling SYNCore 4cd9373 SAFSYN/phase.cpp directly with
    // MSVC /O2, using PCM[i] = (i*997)%20001-10000, 97 frames at 8 kHz,
    // sustain loop [17,78), tick 1234567890123, channel 3, key 60, seed 42.
    // Quadrature/output scale by 32768/32767 for Kestrel's PCM convention.

[2] src/phase/tests.rs:502
/// Everything the driver hands a backend that `materialise` decides, and no
/// rendering: nothing is ever alive, so every block admits all it can.

[3] src/phase/tests.rs:552
/// `Driver::materialise` builds a big block's voices on several threads, each
/// into its own slots, so what reaches the backend has to be the same bytes on
/// any number of them. With analytic phase on and off, with every layer
/// delayed into a later block, with a channel gliding, and with the block
/// both inside the pool and past it, where ranking reorders it.

[4] src/phase/tests.rs:569
                // 40,000 note-ons of two layers each, inside one 128-frame
                // block, on every channel; channel 1 glides.
